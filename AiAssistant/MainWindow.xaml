<Window x:Class="AiAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AiAssistant" Width="280" Height="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="CanResize"
        MinWidth="160" MinHeight="100"
        WindowStartupLocation="CenterScreen"
        ShowActivated="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseRightButtonUp="Window_MouseRightButtonUp">
    <Grid>
        <Border Width="260" Height="80" CornerRadius="12"
                HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="#CC1E90FF" Padding="8">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8">
                <Ellipse Width="48" Height="48" Fill="White" Opacity="0.95"/>
                <TextBlock Text="桌面助理" Foreground="White" FontWeight="Bold" FontSize="18" VerticalAlignment="Center"/>
                <Button Content="Test" Width="56" Height="28" VerticalAlignment="Center" Click="OnTestButtonClick"/>
            </StackPanel>
        </Border>

        <!-- 底右角固定大小 resize handle（不會跟 UI 一起放大） -->
        <Thumb x:Name="ResizeThumb"
               Width="18" Height="18"
               HorizontalAlignment="Right" VerticalAlignment="Bottom"
               Margin="8"
               Cursor="SizeNWSE"
               DragStarted="ResizeThumb_DragStarted"
               DragDelta="ResizeThumb_DragDelta"
               DragCompleted="ResizeThumb_DragCompleted">
            <Thumb.Template>
                <ControlTemplate TargetType="Thumb">
                    <Grid Width="{TemplateBinding Width}" Height="{TemplateBinding Height}">
                        <Path Data="M2,16 L16,2 M7,16 L16,7" Stroke="White" StrokeThickness="1.4" Opacity="0.9"/>
                    </Grid>
                </ControlTemplate>
            </Thumb.Template>
        </Thumb>
    </Grid>
</Window>