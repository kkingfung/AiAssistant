<Window x:Class="FloatingAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FloatingAssistant"
        Width="280" Height="120"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Grid>
        <!-- 內容區塊為半透明的圓角面板；視窗背景維持完全透明 -->
        <Border Width="260" Height="80" CornerRadius="14" HorizontalAlignment="Center" VerticalAlignment="Center"
                Background="#CC1E90FF"  >
            <StackPanel Orientation="Horizontal" Margin="12">
                <Ellipse Width="48" Height="48" Fill="White" Opacity="0.95"/>
                <TextBlock Text="桌面助理" Foreground="White" FontWeight="Bold" FontSize="18" VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>